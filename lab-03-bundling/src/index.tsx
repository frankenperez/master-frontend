import * as React from "react";
import * as ReactDOM from "react-dom";

import { BoxMessage } from "AppComponents";

ReactDOM.render(
  <BoxMessage message="Hello World from React!" />,
  document.getElementById("app")
);
